---
layout: post
title:  "SpringMvc"
date:   2019-10-01 22:05:00
categories: 其它
---


## 待学习

* [Spring](https://www.w3cschool.cn/wkspring/pesy1icl.html)
* [Jdbc教程](https://www.yiibai.com/jdbc)

## Spring

**注解**
* @Controller：声明为一个bean
* @Required 注释应用于 bean 属性的 setter 方法，它表明受影响的 bean 属性在配置时必须放在 XML 配置文件中，否则容器就会抛出一个 BeanInitializationException 异常。
* @Autowired 注释可以在 setter 方法中被用于自动连接 bean，就像 @Autowired 注释，容器，一个属性或者任意命名的可能带有多个参数的方法。

**<url-pattern>的匹配**
* 完整路径匹配；
* 根据路径前缀，进行最长匹配，使用'/'作为路径分隔符；
* 使用请求路径中的后缀进行匹配，比如'.jsp'；
* 如果以上都没有匹配到合适的Servlet，将使用默认(default)Servlet来处理请求；

**url-pattern的配置**

* “*.”do访问以.do结尾的由DispatcherServlet进行解析
* “/”所有访问的地址都是由 DispatcherServlet进行解析，对于静态的文件解析需要配置,不让DispatcherServlet进行解析.
* “/*”这样配置不对,使用这种配置,最终要转发到一个jsp页面时,仍然会由DispatcherServlet进行解析,但是不能根据这个jsp页面找到handler所以会报错。 

## mybatis

[学习链接](https://blog.csdn.net/qq_36761831/article/details/90579922)



## 待学习

* scope范围与析构时机